const styles = ({ palette, typography }) => ({
  container: {
    display: 'flex',
    justifyContent: 'flex-end',
    listStyle: 'none',
    margin: 0,
    padding: 0,
    position: 'relative',
    zIndex: 10,
  },
});

export default styles;
