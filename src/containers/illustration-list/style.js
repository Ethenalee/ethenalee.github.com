const styles = ({ typography }) => ({
  image: {
    width: '100%',
    height: '100%',
  },
  mainImage: {
    '@media (max-width: 1024px)': {
      width: '80%',
      margin: 'auto',
    },
  },
});

export default styles;
